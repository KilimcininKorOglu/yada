# Local records, spliced into the server clause of unbound.conf by include.
#
# This comment and the blank lines below exist so a test can prove the tool
# rewrites the file without disturbing what it does not manage.

local-zone: "example.test." transparent
         local-data: "seed.example.test. IN A 10.99.99.1"
